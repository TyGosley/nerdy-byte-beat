<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nerdy Byte Beat</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Play:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
</head>

<body>
  <div id="page-conatiner">
  <div class="flex-column justify-space-around" id="content-wrap">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <span role="img" aria-label="nerd emoji">🤓</span>
        <a id="title" href="/">Nerdy Byte Beat</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">Nerdboard</a> |
        <button class="no-button" id="logout">Nerd Out</button>
        {{else}}
        <a href="/login">Nerd In</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer id="footer">
      Nerdy Byte Beat &copy; 2023 By Be Awesome Productions
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  </div>
</body>

</html>
